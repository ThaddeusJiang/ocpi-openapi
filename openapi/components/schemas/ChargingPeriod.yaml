summary: ChargingPeriod schema
type: object
required:
- start_date_time
properties:
  start_date_time:
    $ref: ./DateTime.yaml
  dimensions:
    $ref: ./CdrDimension.yaml
  tariff_id:
    type: string
    maxLength: 36
    description: Case insensitive string.
