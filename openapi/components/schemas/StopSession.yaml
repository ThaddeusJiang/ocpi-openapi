summary: StopSession schema
type: object
required:
- response_url
- session_id
properties:
  response_url:
    $ref: ./URL.yaml
  session_id:
    type: string
    maxLength: 36
    description: Case insensitive string.
