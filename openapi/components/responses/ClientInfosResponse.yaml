description: ClientInfo list response
headers:
  Link:
    $ref: ../headers/Link.yaml
  X-Total-Count:
    $ref: ../headers/X-Total-Count.yaml
  X-Limit:
    $ref: ../headers/X-Limit.yaml
content:
  application/json:
    schema:
      allOf:
      - $ref: ./BaseResponse.yaml
      - type: object
        required:
        - data
        properties:
          data:
            $ref: ../schemas/ClientInfos.yaml
