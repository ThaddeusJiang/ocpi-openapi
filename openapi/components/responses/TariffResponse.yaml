description: Tariff response
content:
  application/json:
    schema:
      allOf:
      - $ref: ./BaseResponse.yaml
      - type: object
        required:
        - data
        properties:
          data:
            $ref: ../schemas/Tariff.yaml
