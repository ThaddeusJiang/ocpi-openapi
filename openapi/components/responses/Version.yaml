description: OCPI version details response
content:
  application/json:
    schema:
      allOf:
      - $ref: ./BaseResponse.yaml
      - type: object
        required:
        - data
        properties:
          data:
            $ref: ../schemas/VersionDetail.yaml
